{
  "name": "ip-switch",
  "version": "1.0.0",
  "description": "IP、Hosts、代理配置修改器",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "sync-version": "node ./scripts/sync-version.mjs",
    "tauri": "pnpm sync-version && tauri",
    "dev:admin": "powershell -Command \"Start-Process cmd -ArgumentList '/c cd /d %cd% && pnpm tauri dev' -Verb RunAs\""
  },
  "dependencies": {
    "@tauri-apps/api": "^2.0.0"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0",
    "vite": "^5.0.0"
  }
}
